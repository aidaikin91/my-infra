apiVersion: v1
kind: Service
metadata:
  name: search-history
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: search-history
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP
